language: java

env:
- TMPDIR=/tmp

services:
- docker

before_install:
- docker build -t mcapuccini/insight-particle-evaluator ./container
- cd cinsight

after_success:
  provider: script
  script: scripts/deploy.sh
  on:
    branch: master
